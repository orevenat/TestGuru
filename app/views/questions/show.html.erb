<h1>Question: <%= @question.body %></h1>

<ul>
  <% @question.answers.each do |answer| %>
    <li><%= answer.body %></li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_question_path(@question) %></td>
|
<%= link_to 'Back', test_questions_path(@question.test) %></td>
